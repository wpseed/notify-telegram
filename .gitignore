# Composer (the dependencies are needed for development and tests only)
/vendor
composer.lock

# Build output
/dist
/build-src

# Tests
/.phpunit.cache
/phpunit.xml
.phpunit.result.cache

# Front-end sources and the built admin bundle (npm run build; the release archive gets it in CI)
/node_modules
/assets/admin
